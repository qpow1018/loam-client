import styles from './emptyGoldPanel.module.scss';

export default function CharacterGoldPanel() {
  return (
    <section className={styles['empty-gold-panel']}>
      <p>캐릭터별 골드 화면을 준비 중입니다.</p>
    </section>
  );
}
